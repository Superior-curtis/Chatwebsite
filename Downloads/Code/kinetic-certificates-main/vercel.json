{
  "buildCommand": "cd kinetic-certificates-main && npm install && npm run build",
  "outputDirectory": "kinetic-certificates-main/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
